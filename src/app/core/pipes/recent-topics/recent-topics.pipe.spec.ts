import { RecentTopicsPipe } from './recent-topics.pipe';

describe('RecentTopicsPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentTopicsPipe();
    expect(pipe).toBeTruthy();
  });
});
